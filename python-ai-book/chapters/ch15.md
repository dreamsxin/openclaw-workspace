# 第15章 项目：多 Agent 协作系统

> 单个 Agent 能力有限，多个 Agent 协作才能解决复杂问题。

## 15.1 为什么需要多 Agent？

单 Agent 的局限：
- 上下文窗口有限
- 角色混乱（既要分析又要写代码又要审查）
- 无法并行处理

多 Agent 的优势：
- **分工明确**：每个 Agent 专注一件事
- **并行处理**：多个任务同时进行
- **质量保证**：Agent 之间互相审查

## 15.2 协作模式

### 模式一：管道式（Pipeline）

```
需求 → 分析Agent → 设计Agent → 编码Agent → 审查Agent → 输出
```

### 模式二：辩论式（Debate）

```
问题 → Agent A 的方案 ←→ Agent B 的质疑 → 最终方案
```

### 模式三：专家团（Expert Panel）

```
         ┌→ 专家A（前端）→ 建议 ─┐
问题 ────┼→ 专家B（后端）→ 建议 ─┼→ 综合 → 方案
         └→ 专家C（安全）→ 建议 ─┘
```

## 15.3 核心实现

```python
# code/ch15/multi_agent.py
"""多 Agent 协作系统"""

import anthropic
from dataclasses import dataclass, field
from typing import Callable
from concurrent.futures import ThreadPoolExecutor


@dataclass
class Agent:
    """AI Agent 定义"""
    name: str
    role: str
    system_prompt: str
    model: str = "claude-sonnet-4-20250514"

    def __post_init__(self):
        self.client = anthropic.Anthropic()
        self.history = []

    def think(self, message: str, context: str = "") -> str:
        """思考并回复"""
        prompt = message
        if context:
            prompt = f"上下文：\n{context}\n\n任务：{message}"

        self.history.append({"role": "user", "content": prompt})

        response = self.client.messages.create(
            model=self.model,
            max_tokens=2000,
            system=self.system_prompt,
            messages=self.history
        )

        answer = response.content[0].text
        self.history.append({"role": "assistant", "content": answer})
        return answer


@dataclass
class Team:
    """Agent 团队"""
    name: str
    agents: list[Agent] = field(default_factory=list)

    def add_agent(self, agent: Agent):
        self.agents.append(agent)

    def get_agent(self, name: str) -> Agent | None:
        return next((a for a in self.agents if a.name == name), None)


class MultiAgentSystem:
    """多 Agent 协作系统"""

    def __init__(self):
        self.teams: dict[str, Team] = {}
        self.results: dict[str, str] = {}

    def create_team(self, name: str) -> Team:
        team = Team(name)
        self.teams[name] = team
        return team

    def pipeline(self, agents: list[Agent], task: str) -> str:
        """管道模式：顺序执行"""
        current = task

        for agent in agents:
            print(f"  🔄 {agent.name} 处理中...")
            current = agent.think(current)
            self.results[agent.name] = current
            print(f"  ✅ {agent.name} 完成")

        return current

    def debate(self, agents: list[Agent], topic: str, rounds: int = 3) -> str:
        """辩论模式：多轮讨论"""
        discussion = [f"议题：{topic}"]

        for round_num in range(rounds):
            print(f"\n  📢 第 {round_num + 1} 轮辩论")
            round_responses = []

            for agent in agents:
                # 每个 Agent 看到之前的讨论
                context = "\n".join(discussion)
                response = agent.think(
                    f"请从你的专业角度发表看法（第{round_num+1}轮）",
                    context
                )
                round_responses.append(f"[{agent.name}]：{response}")
                print(f"    💬 {agent.name} 发言")

            discussion.extend(round_responses)

        # 综合结论
        synthesizer = Agent(
            name="综合者",
            role="综合各方观点",
            system_prompt="你是一个善于总结的专家，综合各方观点给出最终建议。"
        )

        context = "\n".join(discussion)
        conclusion = synthesizer.think("综合以上讨论，给出最终结论和行动方案", context)
        return conclusion

    def expert_panel(self, experts: list[Agent], question: str) -> str:
        """专家团模式：并行分析，综合结论"""
        print(f"\n  🎯 启动专家团（{len(experts)} 位专家）")

        # 并行获取各专家意见
        with ThreadPoolExecutor(max_workers=len(experts)) as executor:
            futures = {
                executor.submit(agent.think, question): agent.name
                for agent in experts
            }

            opinions = {}
            for future in futures:
                name = futures[future]
                opinions[name] = future.result()
                print(f"    📝 {name} 提交意见")

        # 综合意见
        context = "\n\n".join(
            f"【{name}的意见】\n{opinion}"
            for name, opinion in opinions.items()
        )

        synthesizer = Agent(
            name="决策者",
            role="综合专家意见做出决策",
            system_prompt="你是一个项目负责人，综合各专家意见，做出最终决策。"
        )

        conclusion = synthesizer.think(
            "综合各专家意见，给出最终方案和实施步骤",
            context
        )

        return conclusion
```

## 15.4 实战：软件项目评审

```python
# code/ch15/project_review.py
"""用多 Agent 系统评审软件项目"""

from multi_agent import Agent, MultiAgentSystem


def review_project(project_description: str):
    """用专家团评审项目方案"""

    system = MultiAgentSystem()

    # 定义专家团队
    experts = [
        Agent(
            name="架构师",
            role="系统架构设计",
            system_prompt="""你是一个有10年经验的软件架构师。
关注：系统架构、可扩展性、技术选型、微服务划分。
用专业的角度分析，指出架构层面的风险和建议。"""
        ),
        Agent(
            name="安全专家",
            role="安全审计",
            system_prompt="""你是一个网络安全专家。
关注：数据安全、认证授权、输入验证、常见漏洞（OWASP Top 10）。
从安全角度审查方案，指出潜在的安全风险。"""
        ),
        Agent(
            name="性能专家",
            role="性能优化",
            system_prompt="""你是一个性能优化专家。
关注：响应时间、吞吐量、数据库查询、缓存策略、并发处理。
评估方案的性能瓶颈和优化建议。"""
        ),
        Agent(
            name="产品经理",
            role="用户体验",
            system_prompt="""你是一个产品经理。
关注：用户需求、功能完整性、用户体验、MVP 定义、迭代计划。
从产品角度评估方案的可行性和优先级。"""
        ),
    ]

    # 专家团评审
    result = system.expert_panel(experts, f"""
请评审以下软件项目方案：

{project_description}

请从你的专业角度提供：
1. 方案的优势
2. 潜在风险
3. 改进建议
4. 优先级建议（高/中/低）
""")

    return result


if __name__ == "__main__":
    project = """
项目：在线协作文档编辑器

功能需求：
- 实时多人协同编辑（类似 Google Docs）
- 版本历史和回滚
- 评论和讨论
- 权限管理（查看/编辑/管理员）
- 导出为 PDF/Word

技术栈：
- 前端：React + Yjs（CRDT）
- 后端：Node.js + WebSocket
- 数据库：PostgreSQL + Redis
- 部署：Docker + Kubernetes

预期规模：
- 1000 日活用户
- 平均每文档 5 人协作
"""

    print(review_project(project))
```

## 15.5 实战：自动化代码生成流水线

```python
# code/ch15/code_pipeline.py
"""多 Agent 代码生成流水线"""

from multi_agent import Agent, MultiAgentSystem


def generate_feature(requirement: str) -> dict:
    """用管道模式生成完整功能"""

    system = MultiAgentSystem()

    # 定义流水线
    agents = [
        Agent(
            name="需求分析师",
            role="分析需求",
            system_prompt="你是需求分析专家。将用户需求转化为详细的功能规格说明，包括输入输出、边界情况、验收标准。"
        ),
        Agent(
            name="架构设计师",
            role="设计架构",
            system_prompt="你是架构设计师。根据需求规格，设计代码架构，包括模块划分、接口定义、数据结构。输出详细的设计文档。"
        ),
        Agent(
            name="高级开发",
            role="编写代码",
            system_prompt="你是高级Python开发工程师。根据设计文档，编写生产级代码。要求：类型注解、docstring、错误处理、日志记录。"
        ),
        Agent(
            name="测试工程师",
            role="编写测试",
            system_prompt="你是测试工程师。根据代码和需求，编写全面的测试用例。包括：单元测试、集成测试、边界测试。"
        ),
        Agent(
            name="代码审查员",
            role="审查代码",
            system_prompt="你是代码审查专家。审查代码质量，检查：安全隐患、性能问题、代码规范、最佳实践。输出审查报告。"
        ),
    ]

    # 执行流水线
    print("🚀 启动代码生成流水线\n")
    result = system.pipeline(agents, requirement)

    return {
        "requirement": requirement,
        "final_output": result,
        "agent_outputs": system.results
    }


if __name__ == "__main__":
    req = """
需求：实现一个用户认证系统

功能：
1. 用户注册（用户名、邮箱、密码）
2. 用户登录（JWT Token）
3. 密码重置（邮件验证）
4. Token 刷新
5. 登录日志记录

技术要求：
- 使用 FastAPI
- 密码用 bcrypt 加密
- JWT Token 有效期 24 小时
- 防止暴力破解（限流）
"""

    result = generate_feature(req)
    print("\n" + "=" * 60)
    print("📋 最终输出：")
    print("=" * 60)
    print(result["final_output"])
```

## 15.6 通信机制

### 共享黑板

```python
# code/ch15/blackboard.py
"""Agent 间的共享黑板"""

class Blackboard:
    """共享信息板"""

    def __init__(self):
        self.data = {}
        self.locks = {}

    def write(self, agent_name: str, key: str, value: any):
        """Agent 写入信息"""
        if key not in self.data:
            self.data[key] = []
        self.data[key].append({
            "agent": agent_name,
            "value": value,
            "timestamp": datetime.now().isoformat()
        })

    def read(self, key: str) -> list:
        """读取信息"""
        return self.data.get(key, [])

    def read_latest(self, key: str) -> any:
        """读取最新信息"""
        entries = self.data.get(key, [])
        return entries[-1]["value"] if entries else None

    def subscribe(self, key: str, callback: Callable):
        """订阅信息变更"""
        pass  # 实现省略


# 使用
blackboard = Blackboard()

# Agent A 写入分析结果
blackboard.write("分析师", "data_analysis", analysis_result)

# Agent B 读取并基于此生成代码
analysis = blackboard.read_latest("data_analysis")
```

## 15.7 错误处理与容错

```python
# 容错机制
class ResilientPipeline:
    """容错流水线"""

    def __init__(self):
        self.fallback_agents = {}

    def register_fallback(self, agent_name: str, fallback: Agent):
        """注册备用 Agent"""
        self.fallback_agents[agent_name] = fallback

    def run_with_fallback(self, agents: list[Agent], task: str) -> str:
        current = task

        for agent in agents:
            try:
                current = agent.think(current)
            except Exception as e:
                print(f"  ⚠️ {agent.name} 失败: {e}")

                # 尝试备用 Agent
                fallback = self.fallback_agents.get(agent.name)
                if fallback:
                    print(f"  🔄 使用备用 Agent: {fallback.name}")
                    current = fallback.think(current)
                else:
                    print(f"  ❌ 无备用，跳过")

        return current
```

## 15.8 总结与展望

### 本书回顾

| 部分 | 内容 |
|------|------|
| 基础入门 | 环境搭建、AI 绑定概念、第一次交互 |
| 核心技能 | Prompt 工程、API 调用、上下文管理 |
| Agent 开发 | Agent 架构、工具调用、记忆系统 |
| 实战进阶 | Claude Code、OpenClaw、自动化工作流 |
| 项目实战 | 代码助手、数据分析、多 Agent 协作 |

### 下一步

1. **深入学习**：LangChain、LangGraph、AutoGen
2. **实践项目**：从自己的需求出发，构建实用工具
3. **社区参与**：OpenClaw、Claude 社区
4. **持续关注**：AI 领域变化很快，保持学习

### 核心理念

> **AI 是工具，不是替代。**
> 理解原理，掌握方法，用 AI 增强你的能力，而不是依赖它。

---

**上一章：[第14章 项目：自动化数据分析师](ch14.md)**

---

🎉 **恭喜你完成了《Python AI 绑定》的学习！**

有任何问题，欢迎交流。
