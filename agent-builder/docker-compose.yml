services:
  agent:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - ./config.json:/app/config.json
      - ./.agent_memory:/app/.agent_memory
    restart: unless-stopped
